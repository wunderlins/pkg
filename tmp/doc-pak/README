Unified Package Manager - upkg

The unified Package Manager is an attempt to use the same command line syntax and output with different *NIX package Managers.

This version supports: 
 - apt-get (Debian and syblings), 
 - port (MacPorts) 
 - pacman (Arch).

experimental support
 - yum - unfinished and experimental (redhat and siblings)

planned support
 - pkg (openbsd pkg_add etc.)


$Id: README 152 2011-04-28 18:53:04Z wus $
